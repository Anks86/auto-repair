{
  "name": "babbal-auto-repair-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "test": "node --test",
    "audit": "npm run build && node scripts/final-audit.mjs && npm test && npm run check:functions",
    "qa": "npm run build && node scripts/qa-preview.mjs",
    "dev": "npm run build && wrangler pages dev",
    "check:functions": "wrangler pages functions build --outdir=.wrangler/functions",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=babbal-auto-repair"
  },
  "devDependencies": {
    "wrangler": "4.124.0"
  }
}
